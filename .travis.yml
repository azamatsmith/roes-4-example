language: node_js
node_js:
  - "8"

cache:
  yarn: true
  directories:
    - node_modules

   before_install:
   #   - npm install -g codecov
  
   script:
     - yarn test -- --coverage
  
   # after_success:
   # - codecov --token=
